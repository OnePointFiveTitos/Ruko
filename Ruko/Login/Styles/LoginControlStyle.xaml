<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Login.Styles">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ControlsAndResources;component/Styles/Default/Package.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <Style TargetType="PasswordBox" BasedOn="{StaticResource DefaultPasswordBoxes}"/>
    <Style TargetType="TextBlock" BasedOn="{StaticResource DefaultTextblocks}">
        <Setter Property="TextAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="ErrorStyle" BasedOn="{StaticResource DefaultTextblocks}">
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
</ResourceDictionary>