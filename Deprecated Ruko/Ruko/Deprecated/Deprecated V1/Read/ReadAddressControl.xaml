<UserControl x:Class="Ruko.ReadAddressControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:car="clr-namespace:ControlsAndResources;assembly=ControlsAndResources"
             xmlns:local="clr-namespace:Ruko">
    <StackPanel>
        
        <car:LabeledTextblock
            Label="Street: "
            Input="{Binding Street}"/>

        <car:LabeledTextblock
            Label="City: "
            Input="{Binding City}"/>

        <car:LabeledTextblock
            Label="State: "
            Input="{Binding State}"/>

        <car:LabeledTextblock
            Label="Zipcode: "
            Input="{Binding ZipCode}"/>
        
    </StackPanel>
</UserControl>